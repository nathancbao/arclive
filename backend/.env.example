# Copy to .env and fill in your Supabase (or any Postgres) connection string.
# The driver prefix MUST be postgresql+asyncpg:// for async SQLAlchemy.
#
# Supabase connection string format:
#   postgresql+asyncpg://<user>:<password>@<host>:5432/<dbname>
#
# You can find this in: Supabase → Project Settings → Database → Connection string (URI)
# Replace "postgres://" with "postgresql+asyncpg://"

DATABASE_URL=postgresql+asyncpg://user:password@db.xxxx.supabase.co:5432/postgres
